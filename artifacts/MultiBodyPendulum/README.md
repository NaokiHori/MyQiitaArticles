# Pendulum

## Dependency

- C compiler

- GNU Make

## Quick start

Normal use:

```
make clean
make output
make datadel
make all
./a.out
```

If `Xlib` is properly installed, the motion can be visualised simultaneously:

```
make clean
make output
make datadel
make all-x
./a.out
```
